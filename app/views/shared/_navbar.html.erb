<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <%= link_to root_path do %>
      <%= image_tag "fake_airbnb.png", class: "me-3", style: "height: 40px;" %>
    <% end %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="ms-auto d-flex align-items-center">
        <form class="d-flex my-2 my-lg-0">
          <input class="form-control me-2" name="query" type="search" placeholder="Search" aria-label="Search" value="<%= params[:query] %>">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
        <%= link_to "Add Listing", new_listing_path, class: "btn btn-outline-primary my-2 my-sm-0 ms-2" %>

        <% if user_signed_in? %>
          <div class="d-flex">
            <%= button_to "Logout", destroy_user_session_path, class: "btn btn-outline-primary my-2 my-sm-0 ms-2", method: :delete %>
            <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-outline-primary my-2 my-sm-0 ms-2" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
<hr style="height: 1px; background-color: #adb5bd; border: none; margin: 0; width: 100vw; position: relative; left: 50%; transform: translateX(-50%);">
